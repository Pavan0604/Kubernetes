apiVersion: v1 
kind: ResourceQuota
metadata: 
  name: mem-cpu-resource-quota
  namespace: nginx-namespace
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "2"
    limits.memory: 2Gi