apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: catalog-ebs-volume-claim
  namespace: catalog
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-gp3
  resources:
    requests:
      storage: 10Gi
